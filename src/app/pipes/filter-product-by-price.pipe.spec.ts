import { FilterProductByPricePipe } from './filter-product-by-price.pipe';

describe('FilterProductByPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductByPricePipe();
    expect(pipe).toBeTruthy();
  });
});
